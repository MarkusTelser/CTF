First thing is checking out the logs of git. Then I find out that there are 3 commits being made. Than checking out all the commits and so I found the flag in commit 2.

flag = flag{protect_your_git}
